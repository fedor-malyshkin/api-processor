# action.yml
name: 'API Processor'
description: 'Process Protobuff specification to generate variety endpoints (in Go, JS)'
inputs:
  github_token:
    description: "GITHUB_TOKEN. Optional if you use checkout@v2 action."
    default: "${{ github.token }}"
  proto-spec-dir-location:  # id of input
    description: 'The directory with .proto files'
    required: false
    default: 'specs'
  api-dir-location:  # id of input
    description: 'The directory with generated API files'
    required: false
    default: 'api'    
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.proto-spec-dir-location }}
    - ${{ inputs.api-dir-location }}    
branding:
  icon: "circle"
  color: "green"
